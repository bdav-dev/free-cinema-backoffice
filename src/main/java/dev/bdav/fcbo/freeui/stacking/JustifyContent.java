package dev.bdav.fcbo.freeui.stacking;

public enum JustifyContent {
    NONE, // default
    TOP, // only availabe on VStack
    BOTTOM, // only availabe on VStack
    LEFT, // only availabe on HStack
    RIGHT, // only availabe on HStack
    CENTER,
    SPACE_BETWEEN,
    SPACE_AROUND
}
